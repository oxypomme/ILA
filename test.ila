// Ceci est un commentaire (slt du coup)
// Ce fichier est là pour tester et donne un exemple fonctionnel

//ici on déclare les variables de l'algo
nombre:entier // Nombre à traiter
resultat:entier // Carré calculé
pi:const réel <- 3.141592 //une constante
tableau[taille]:entier //tableau d'entiers

//Un seul algo par fichier ! > Pour differencier l'algo principal d'un module ou autre
algo Carre{
    lire(nombre)
    resultat <- fnc_carre(nombre)
    modu_resultat(resultat, message)
    ecrire(message)
}

fonction fnc_carre(nombre:entier):entier{
    fnc_carre <- nombre * nombre
}

module modu_resultat(e::resultat:entier, s::message:chaine){
    ecrire(resultat)
    message <- "yay"
}

//structure
structure personne{
    nom:chaine
    age:entier
}

/*
opérateurs : + - * / mod div et ou non <- < <= > >= = !=


*/